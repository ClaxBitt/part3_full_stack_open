{
  "name": "part3_full_stack_open",
  "version": "1.0.0",
  "description": "Exercise part 3 of Full Stack Course",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../Part2/part2_full_stack_open/phonebook/ && npm run build && cp -r dist ../../Part3/part3_full_stack_open",
    "build:full": "npm run build:ui && git add . && git commit -m uibuild & git push origin main"
  },
  "author": "ClaxBitt",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
